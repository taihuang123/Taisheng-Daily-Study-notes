1、static inline :

    static: 静态函数，简单来说，文件内可访问，允许其它文件中存在同名函数。

    inline：向编译器建议，将函数以内联的方式嵌入到函数调入点。
    
    static inline： inline的生效权由编译器控制，如果生效，会类似于宏定义将函数替换到调用者指令。
    好处：减少调用函数开销，减少传参数时引起的压栈出栈的开销。减少pc 跳转时对流水线的破坏（？不太懂）。
    坏处：代码体积大（其实没吊影响）。
